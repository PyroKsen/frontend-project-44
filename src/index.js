import readlineSync from 'readline-sync';

import { hi } from '../src/cli.js';
const name = hi()

export const win = () => {
    if (i === 3) {
        console.log('Congratulations, ' + name + '!')
    }
}
